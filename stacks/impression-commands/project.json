{
    "name": "impression-commands",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "projectType": "application",
    "sourceRoot": "stacks/impression-commands/src",
    "targets": {
        "build": {
            "executor": "@nrwl/workspace:run-commands",
            "options": {
                "cwd": "stacks/impression-commands",
                "color": true,
                "command": "sls package"
            }
        },
        "serve": {
            "executor": "@nrwl/workspace:run-commands",
            "options": {
                "cwd": "stacks/impression-commands",
                "color": true,
                "command": "sls offline start"
            }
        },
        "deploy": {
            "executor": "@nrwl/workspace:run-commands",
            "options": {
                "cwd": "stacks/impression-commands",
                "color": true,
                "command": "sls deploy"
            }
        },
        "remove": {
            "executor": "@nrwl/workspace:run-commands",
            "options": {
                "cwd": "stacks/impression-commands",
                "color": true,
                "command": "sls remove"
            }
        },
        "lint": {
            "executor": "@nrwl/linter:eslint",
            "options": {
                "lintFilePatterns": ["stacks/impression-commands/**/*.ts"]
            }
        },
        "test": {
            "executor": "@nrwl/jest:jest",
            "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
            "options": {
                "jestConfig": "stacks/impression-commands/jest.config.ts",
                "passWithNoTests": true
            }
        }
    },
    "tags": ["stack"]
}
